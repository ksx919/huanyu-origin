<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tanxian.mapper.UserMapper">

    <select id="findByEmail" parameterType="string" resultType="com.tanxian.entity.User">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
    </select>

    <update id="updateLastLoginTime">
        UPDATE users SET last_login_at = #{lastLoginTime}, updated_at = #{lastLoginTime} WHERE id = #{userId}
    </update>
</mapper>